import { Component } from "ape-ecs";

/**
 * Skip follow-up calculations and guarantee a follow-up (not a counterattack).
 */
class GuaranteedFollowup extends Component { };

export default GuaranteedFollowup;
