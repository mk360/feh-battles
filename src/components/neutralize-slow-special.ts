import { Component } from "ape-ecs";

/**
 * If Hero has this component and a "Slow Special" effect on them, remove them both.
 */
class NeutralizeSlowSpecial extends Component { };

export default NeutralizeSlowSpecial;
