import path from "path";
import GameWorld from "../src/world";
import test from "node:test";
import { strictEqual } from "assert";

test("this is a test runner", () => {
    strictEqual(1 + 1, 12, "1 + 1 is supposed to be 2")
})